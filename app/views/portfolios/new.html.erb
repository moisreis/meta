<%#
  # View: new.html.erb
  # Date: 11/20/2025
  # Author: Moisés Reis
  # Package: Portfolio
  #
  # Description: Renders the page dedicated to creating a new Portfolio resource. It initializes
  #              a blank Portfolio object and displays the required input fields via the
  #              shared form partial.
  #
  # Usage: - What: Renders the complete view for creating a new Portfolio instance.
  #                It is responsible for wrapping the input form within the standard
  #                dashboard layout.
  #        - How: Renders the `_wrapper` for the page layout. It then delegates
  #               rendering of the input fields to the reusable `_form` partial,
  #               passing the new `@portfolio` object and the customized "Adicionar"
  #               (Add) button text and icon.
  #        - Why: Provides the interface necessary to execute the 'Create' function
  #               (C in CRUD) for the Portfolio resource using a consistent UI structure.
  %>
<%= render 'partials/route/dashboard/wrapper', page_title: "Criar nova carteira", page_desc: "Defina os detalhes da sua nova carteira para começar a acompanhar seus investimentos." do %>
  <%= render "form", portfolio: @portfolio, button_name: "Adicionar", icon_name: "add" %>
<% end %>