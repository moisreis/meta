<%#
  # View: edit.html.erb
  # Date: 11/20/2025
  # Author: Moisés Reis
  # Package: Portfolio
  #
  # Description: Renders the page for updating an existing Portfolio record. It displays the
  #              edit form and incorporates a separate, irreversible action section for
  #              deleting the entire portfolio and its associated data.
  #
  # Usage: - What: Renders the full view dedicated to editing an existing Portfolio.
  #                It provides the necessary form for updating attributes and a distinct
  #                "Danger Zone" section for permanently deleting the resource.
  #        - How: Renders the `_wrapper` for layout, passing the current portfolio's name as the title.
  #               It uses the `_form` partial for input and a custom `_card` partial
  #               to wrap the `button_to` helper, which performs the DELETE action.
  #        - Why: Provides the interface necessary to execute the 'Update' and 'Delete'
  #               functions (U and D in CRUD) for the Portfolio resource.
  %>
<%= render 'partials/route/dashboard/wrapper',
           page_title: @portfolio.name,
           page_desc: "Atualize as informações da sua carteira conforme necessário." do
%>
  <%= render "form",
             portfolio: @portfolio,
             button_name: "Atualizar",
             icon_name: "refresh"
  %>
  <%= render 'partials/blocks/card',
             bg_path: "pixeltrue/bomb-9.svg",
             is_danger: true,
             card_title: "Excluir carteira".html_safe,
             card_desc: "Você deseja deletar esta carteira e todos os seus dados? Esta ação é irreversível" do
  %>
    <%= button_to @portfolio,
                  method: :delete,
                  class: "button button-danger w-fit",
                  data: { turbo_confirm: "Are you sure?" } do
    %>
      <%= inline_svg_tag "icons/delete_forever.svg" %>
      <%= content_tag :span, "Deletar minha conta" %>
    <% end %>
  <% end %>
<% end %>
