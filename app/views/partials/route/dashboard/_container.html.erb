<%#
  # Partial: _container.html
  # Date: 11/14/2025
  # Author: Moisés Reis
  # Package: Meta
  #
  # Description: Wraps a page section that displays a title, a subtitle,
  #              and a styled content area. Provides consistent spacing,
  #              colors, and layout structure across profile-editing views.
  #
  # Usage: - What: Encapsulates a heading block, a separator,
  #                and a content container for form elements or components.
  #        - How:  Render this partial and pass inner content through `yield`
  #                to compose custom interfaces inside the standardized layout.
  #        - Why:  Ensures layout consistency, reduces repetition,
  #                and centralizes styling for editable user pages.
  %>
<section class="p-9 bg-accent flex flex-col gap-9 rounded-b-[24px]">
  <div class="flex flex-col gap-0">
    <%= content_tag :h1, "Editar perfil", class: "text-heading font-heading text-2xl font-semibold text-left" %>
    <%= content_tag :p, "Faça alterações em seus dados de usuário", class: "text-muted font-body text-base font-normal text-left" %>
  </div>
  <%= render "partials/modules/separator" %>
  <div class="bg-white border border-border rounded-base shadow-xs p-12 flex flex-col gap-12">
    <%= yield %>
  </div>
</section>